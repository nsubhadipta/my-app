import { StyleSheet } from 'react-native';
const globals = StyleSheet.create({
  container:{
    padding:24
  }
});
 const loginStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
 const loginStyles1 = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });



  export default {globals, loginStyles, loginStyles1}